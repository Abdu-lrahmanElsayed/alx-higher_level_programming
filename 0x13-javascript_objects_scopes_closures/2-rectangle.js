#!/usr/bin/node
class Rectangle {
  constructor (w, h) {
    if (w <= 0) {
	    this.width;
	    this.height;
    } else if (h <= 0) {
	    this.width;
	    this.height;
    } else {
    	this.width = w;
    	this.height = h;
    }
  }
}

module.exports = Rectangle;
